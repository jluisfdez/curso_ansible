Sintaxis Yaml

EJ, dada una tarea:

- name: Copiar fichero
#  modulo: argumentos con =
  copy: src=/etc/hosts dest=/etc/hosts owner=root group=root mode=0644

ó 

- name: Copiar fichero
#  modulo: argumentos con =
  copy: 
    src: /etc/hosts 
    dest: /etc/hosts 
    owner: root 
    group: root 
    mode: 0644

Tambien se pueden poner una lista de variables para hacer tareas repetitivas:

Ej: Lista de variables
instalar: [ "apache2" , "mysql" , "php5" ]
ó
instalar:
  - apache2
  - mysql
  - php5


Diccionarios  ( instalar.web ) 
Ej: instalar: { web: 'apache2' , bd: 'mariadb' , script: 'php5' }
ó
instalar:
  web: apache2
  bd: mariadb
  script: php5


Pipe/tuberia
    - mitexto: |
        primera linea
        segunda lina
        tercera linea

> para textos de mas de una linea
    - milinealarga: >
        inicio linea
        continuar lina
        fin linea


Modo debug
  - debug: "un texto:
  - debug: var=variable


root@debchrom:~/playbooks# cat  sintaxis.yml
- name: Crear fichero usando variables
  hosts: localhost
  connection: local
  vars:
    - miip: "1.2.3.4"
    - milista: [ "apache2","mariadb","php5"]
    - milistalarga:
      - apache2
      - mariadb
      - php5
    - midic: { web: "apache2" , bd: 'mariadb' , script: 'php5' }
    - midiclargo:
      - web: apache2
      - bd: mariadb
      - script: php5
    - mitexto: |
        primera linea
        segunda lina
        tercera linea
    - milinealarga: >
        inicio linea
        continuar lina
        fin linea
  tasks:
    - debug: var=miip
    - debug: var=milista
    - debug: var=milistalarga
    - debug: var=midic
    - debug: var=midic.web
    - debug: var=midiclargo
    - debug: var=mitexto
    - debug: var=milinealarga
root@debchrom:~/playbooks#

